import 'package:flutter/material.dart';

class ToBeAdded extends StatelessWidget {
  const ToBeAdded({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("To be added"),
      ),
    );
  }
}
